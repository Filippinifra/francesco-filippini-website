import Styled from "styled-components";
import colors from "../../constants/colors";

export const ContainerDivider = Styled.div`
    position: unset !important;
    height: 50px;
    background-color: ${colors.dividerBgColor};
`;
